void main () {
    newVar integer numTermos;
    numTermos = System.readint ();

    newVar integer resultado;
    resultado = fibonnaci(numTermos);
}

func integer fibonnaci (integer seq) {
    if (seq < 2) then {
        return seq;
    };
    System.output (seq);
    return fibonnaci (seq - 1) + fibonnaci (n - 2);
}